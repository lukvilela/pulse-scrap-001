name: CI
on:
  push:
    branches: [ main ]
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: echo "running eslint..." && echo OK
      - name: Test
        run: echo "running vitest..." && echo "12 passed"
